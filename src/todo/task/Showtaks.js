// const ShowTask = ({ tasks }) => {
//   console.log('hello text', tasks);
//   return (
//     <div>
//       {tasks ? (
//         <ul>
//           {tasks.map((task) => (
//             <li key={task.id}>{task.text}</li>
//           ))}
//         </ul>
//       ) : (
//         <p> no tasks found</p>
//       )}
//     </div>
//   );
// };

// export default ShowTask;
